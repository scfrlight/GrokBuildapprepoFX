# Operator safety guide

Sequence 13 operator plane: Telegram Bot API refused (`RealTelegramTransport` raises). HITL cannot skip PM4. Denied commands are audited.

Observe-only verbs are the valid path in Sequence 14. Auto-rearm and auto-promote are forbidden.
