# MT5 Demo boundary

Sequence 11 package is `mt5_execution_engine`. Tickets are `DEMO-*` and are **not** broker truth. PM5 tickets are `SIM-*` and are not broker truth.

Real MT5 terminal send is forbidden in Sequence 14. Venue absence is `unavailable`, never reconciliation pass.

PM6 remains `pm6_post_trade` only.
