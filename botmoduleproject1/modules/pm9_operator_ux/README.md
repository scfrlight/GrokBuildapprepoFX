# pm9_operator_ux

Compatibility re-export of `pm8_operator` (canonical Sequence 13).

The Sequence 00 map named this context PM9. An early build numbered the
operator control plane as Sequence 10 / PM8 — that numbering was wrong.
Canonical home is Sequence 13. Import `botmoduleproject1.modules.pm8_operator`
in new code. See `docs/SEQUENCE_CORRECTION.md`.
