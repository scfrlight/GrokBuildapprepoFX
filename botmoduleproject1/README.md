# Python package `botmoduleproject1`

Import root for the trading system. Sequence 00 contains no runtime behaviour.

Allowed later: composition in `app/`, contracts in `contracts/`, modules under `modules/`.
Forbidden now: strategies, MT5 calls, Telegram, risk math, order send.
