# Tests

- `unit/` — isolated + PM2 + PM3-Strategy Engine
- `contract/` — schema and layer rules
- `integration/` — fakes, never live broker by default
- `e2e/` — future doctor/demo-observe paths

Sequence 04: 159 tests. ADR-008: sandbox pytest on CPython 3.10.21; production floor remains 3.11+.
