# Tests

- `unit/` — isolated
- `contract/` — schema and layer rules (Sequence 01)
- `integration/` — fakes, never live broker by default
- `e2e/` — future doctor/demo-observe paths

Sequence 00 only asserts package import and live-disabled default.
