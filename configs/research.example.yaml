# Offline research overlay (future PM9a). No broker, no orders.

extends: base.example.yaml

profile: research

app:
  environment: research

safety:
  trading_mode: research
  live_trading_enabled: false

feature_flags:
  fine_tune_studio: false
  strategy_engine: false
  forecasting: false
  execution: false
  telegram: false
  live_trading: false

mt5:
  enabled: false
