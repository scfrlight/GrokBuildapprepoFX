# Deterministic test overlay.

extends: base.example.yaml

profile: test

app:
  environment: test

safety:
  trading_mode: test
  live_trading_enabled: false

clock:
  source: fake

mt5:
  enabled: false

persistence:
  enabled: false

telegram:
  enabled: false

logging:
  level: DEBUG
  json: false
